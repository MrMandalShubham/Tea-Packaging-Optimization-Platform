# Public frontend config.
# Everything here ships to the browser in the JS bundle — no secrets, ever.
# The OpenAI key lives on the backend and is reached via POST /api/chat.
NEXT_PUBLIC_API_URL=http://localhost:8000
